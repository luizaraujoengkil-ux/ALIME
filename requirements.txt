streamlit>=1.32
pandas>=2.0
numpy>=1.24
plotly>=5.18
networkx>=3.1
openpyxl>=3.1
folium>=0.15
streamlit-folium>=0.18
shapely>=2.0
pyproj>=3.6
geopandas>=0.14
requests>=2.31
tabulate>=0.9
scikit-learn>=1.3  # usado pelo osmnx.distance.nearest_nodes (busca do nó mais próximo)
markdown>=3.5      # relatório Markdown -> HTML renderizado
xhtml2pdf>=0.2.11  # relatório HTML -> PDF (Python puro, sem binários de sistema)

# OSMnx — malha viária real do OpenStreetMap.
# A partir da 2.x usa geopandas(>=1.0)/pyogrio/shapely, sem GDAL/Fiona
# obrigatórios, viável no Streamlit Cloud. O ALIME degrada graciosamente
# (grafo simplificado k-vizinhos) caso a importação falhe.
osmnx>=2.0
